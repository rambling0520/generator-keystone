html
	head
	body
		p %recipient.firstName%様
		p.text-larger 「#{brand}」にお問い合わせがありましたのでご確認下さい。
		if enquiry.name.full || enquiry.email
			p.text-larger 氏名（連絡先）：
				if enquiry.name.full
					| <strong> #{enquiry.name.full}</strong>
				if enquiry.name.full && enquiry.email
					|  （
				if enquiry.email
					| <a href="mailto:#{enquiry.email}">#{enquiry.email}</a>
				if enquiry.name.full && enquiry.email
					| ）
		//- if enquiry.phone
		//- 	p.text-larger 電話番号: #{enquiry.phone}
		if enquiry.enquiryType
			p.text-larger 種別：#{enquiry._.enquiryType.format()}
		if enquiry.message
			.text-larger 本文：<br/>!{enquiry.message.html}
		p.text-muted 送信日時 #{enquiry._.createdAt.format()}
		p &nbsp;
